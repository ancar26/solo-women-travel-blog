# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors / IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Node (if you ever add build tools)
node_modules/
npm-debug.log*

# Local app settings (Claude / Cowork)
.claude/

# Large image-original backups — keep these OFF the repo, store elsewhere
*.tar
*.tar.gz
images-originals-backup*

# ElevenLabs voiceover scripts — local only, never push
elevenlabs-scripts.txt

# NOTE: images/ is intentionally tracked in git.
# This is a static site — git push = deploy. Images must be in the repo.
# If the repo grows too large, consider git-lfs or a CDN (Cloudinary, etc.).
